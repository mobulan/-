//先将原下一个节点的地址给新节点的尾指针
s->next = p->next;
//再将原节点的地址给新节点的头指针
s->prior = p;
//原来后一个节点的头指针改为新节点
p->next->prior = s;
//最后把原节点的尾指针改为新节点
p->next = s;


//把原节点前一个的尾指针指向原节点后一个
p->next = q->next;
//原节点后一个节点的头指针指向前一个
q->next->prior = p;
free(q)